console.log("🚀 DevSetup - Environnement de développement initialisé");

// Exemple simple pour tester nodemon
let counter = 0;

setInterval(() => {
  counter++;
  console.log(`⏱️ Application en cours d'exécution... tick ${counter}`);
}, 3000);
